/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { DayActivityRecordResponse } from '../models/day-activity-record-response';

/**
 * Dashboard streak / activity / days-worked-out payload for the signed-in athlete.
 * Drives three independent UI widgets: the day-streak pill, the activity dot-line, and the
 * days-worked-out pill. All date math is resolved in the athlete's profile timezone.
 */
export interface StreakActivityResponse {

  /**
   * Current consecutive-day completion streak counting back from `today` (or `today − 1`
   * if today has no completion yet — one-day grace). Always ≥ 0. Capped implicitly by the 90-day
   * query window. Stops at `StreakAndDayCountStartDate` (inclusive) when that floor is set.
   */
  currentStreak?: number;

  /**
   * Number of distinct calendar dates inside `[WindowStart, Today]` on which the athlete completed at least one workout. The client hides the days-worked-out pill when this is 0.
   */
  daysActive?: number;

  /**
   * Length of the days-worked-out window in days, inclusive of both endpoints. Equals `(Today − WindowStart) + 1`.
   */
  daysInWindow?: number;

  /**
   * True when `CurrentStreak > 0`. The client hides the streak pill unless this is true and the streak is ≥ 2.
   */
  isActiveStreak?: boolean;

  /**
   * Per-day activity timeline ordered newest-first. Covers the range from the oldest non-`None`
   * day within the last 90 days through `Today` (leading empty days are trimmed). Empty list
   * when the athlete has no non-dismissed activity in the last 90 days — never null.
   */
  recentDays?: Array<DayActivityRecordResponse> | null;

  /**
   * The calendar date treated as "today" for this response, in the athlete's profile timezone.
   */
  today?: string;

  /**
   * Inclusive start of the days-worked-out window. Equals `StreakAndDayCountStartDate`
   * when the athlete has set it (and it is on or before `today`); otherwise `today − 29`
   * (the 30-day rolling fallback).
   */
  windowStart?: string;
}
