/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { CalendarDayResponse } from '../models/calendar-day-response';
import { DashboardWorkoutCardResponse } from '../models/dashboard-workout-card-response';

/**
 * Response payload for `GET /api/webapp/schedule/calendar/v1`. Drives the web app's
 * six-row calendar grid plus the selected-date workout list in a single round trip.
 */
export interface CalendarScheduleResponse {

  /**
   * Exactly 42 entries, one per visible grid cell, ascending by FitEpic.Api.Models.WebApp.Response.CalendarDayResponse.Date.
   * Use the per-day boolean flags to render the orange/green indicator dots without inspecting
   * the full FitEpic.Api.Models.WebApp.Response.CalendarScheduleResponse.Workouts array.
   */
  days?: Array<CalendarDayResponse> | null;

  /**
   * Requested month (1..12), echoed for client-side correlation.
   */
  month?: number;

  /**
   * Today's calendar date in the athlete's profile timezone. Clients should use this value
   * (not the browser's `Date.now()`) when deciding whether "today" falls inside the
   * displayed window so timezone behavior matches the server.
   */
  today?: string;

  /**
   * Last cell of the visible grid. Equals FitEpic.Api.Models.WebApp.Response.CalendarScheduleResponse.WindowStart + 41 days.
   */
  windowEnd?: string;

  /**
   * First cell of the visible 6×7 grid — always a Sunday. May fall in the prior month when
   * the 1st of FitEpic.Api.Models.WebApp.Response.CalendarScheduleResponse.Month is not itself a Sunday.
   */
  windowStart?: string;

  /**
   * Every non-dismissed scheduled workout visible to the caller whose
   * FitEpic.Api.Models.WebApp.Response.DashboardWorkoutCardResponse.ScheduledDate falls inside
   * FitEpic.Api.Models.WebApp.Response.CalendarScheduleResponse.WindowStart..FitEpic.Api.Models.WebApp.Response.CalendarScheduleResponse.WindowEnd (inclusive). Ordered ascending by
   * `ScheduledDate`, then Pending before Completed, then by workout name.
   * Empty array (not null) when the window has no rows.
   */
  workouts?: Array<DashboardWorkoutCardResponse> | null;

  /**
   * Requested year, echoed for client-side correlation against late responses.
   */
  year?: number;
}
