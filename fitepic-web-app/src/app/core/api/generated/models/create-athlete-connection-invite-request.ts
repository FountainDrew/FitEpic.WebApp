/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { ConnectionPermissionLevel } from '../models/connection-permission-level';

/**
 * Represents a single connection invite to be created during a sync operation.
 * The requesting athlete provides the granting athlete's email address;
 * the API resolves it to an athlete ID server-side.
 */
export interface CreateAthleteConnectionInviteRequest {

  /**
   * The email address of the athlete being invited (the granting athlete).
   * The API resolves this to an athlete ID. Returns 404 if no registered athlete matches.
   */
  grantingAthleteEmail: string;

  /**
   * The mobile-generated UUID for this invite.
   */
  id: string;

  /**
   * The permission level being requested. One of `ViewWorkouts` or `ProgramWorkouts`.
   */
  permissionLevel: ConnectionPermissionLevel;
}
