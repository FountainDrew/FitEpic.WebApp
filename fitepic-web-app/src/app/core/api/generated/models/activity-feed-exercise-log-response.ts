/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */


/**
 * A single logged set or round the athlete recorded for an exercise within a completed workout.
 */
export interface ActivityFeedExerciseLogResponse {

  /**
   * Actual calories the athlete achieved on an erg machine. Populated when the exercise's PerRoundMetric is Calories. Null otherwise.
   */
  actualCalories?: number | null;

  /**
   * Actual distance the athlete covered. Populated when the exercise's PerRoundMetric is Distance. Null otherwise.
   */
  actualDistance?: number | null;

  /**
   * Unit for ActualDistance. One of: Meters, Kilometers, Miles, Feet. Null when ActualDistance is null.
   */
  actualDistanceUnit?: string | null;

  /**
   * Actual duration achieved in whole seconds (e.g. 120 for a 2-minute row). Populated when the exercise's PerRoundMetric is Duration. Null otherwise.
   */
  actualDurationSeconds?: number | null;

  /**
   * Actual reps completed, as a free-form string (e.g. "10", "10/8/6", "AMRAP"). Null when not applicable.
   */
  actualReps?: string | null;

  /**
   * Actual weight lifted in pounds. Populated when the exercise's PerRoundMetric is Weight. Null otherwise.
   */
  actualWeightLbs?: number | null;

  /**
   * Per-row athlete note. Also carries the value when the exercise's PerRoundMetric is CustomValue. Null when not provided.
   */
  notes?: string | null;

  /**
   * 0-based display order of this log entry within the scheduled workout.
   */
  orderIndex?: number;

  /**
   * 1-based round identifier for round-based workouts (AMRAP / EMOM / Intervals / Tabata / Circuit). Null for straight-set workouts.
   */
  roundNumber?: number | null;

  /**
   * 1-based set identifier for straight-set workouts. Null for round-based workouts (AMRAP / EMOM / Intervals / Tabata / Circuit).
   */
  setNumber?: number | null;
}
