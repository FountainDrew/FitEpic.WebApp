/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { WorkoutDeletionCountsResponse } from '../models/workout-deletion-counts-response';

/**
 * Outcome of a personal-workout delete. Unlike the preview, these counts are authoritative — they
 * are exactly what the cascade transaction wrote.
 */
export interface WorkoutDeletionResultResponse {

  /**
   * Per-table counts of the rows soft-deleted, excluding the workout row itself.
   */
  deleted?: WorkoutDeletionCountsResponse | null;

  /**
   * Server timestamp written to `updatedAt` on every row the cascade touched.
   */
  deletedAt?: string;

  /**
   * The workout that was soft-deleted.
   */
  workoutId?: string | null;
}
