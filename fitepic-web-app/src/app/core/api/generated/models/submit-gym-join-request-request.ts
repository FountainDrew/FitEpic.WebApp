/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { GymRole } from '../models/gym-role';

/**
 * Request body for `POST /api/gyms/join-requests`. An athlete submits this to request
 * joining a gym (and being granted a specific role) by entering the gym's public code.
 */
export interface SubmitGymJoinRequestRequest {

  /**
   * The 6-character gym code shared by an Owner or Admin.
   */
  gymCode: string;

  /**
   * Optional client-generated UUID for this join request. When supplied, the endpoint is
   * idempotent: a repeat call with the same `Id` by the same athlete returns the
   * existing row instead of creating a duplicate. Required by the mobile sync outbox pattern
   * for safe replay after partial network failures. When omitted, the server generates an Id.
   */
  id?: string | null;

  /**
   * The role the athlete is requesting. Must be `Athlete` or `Coach`.
   * `Admin` is rejected with 403 — Admin can only be granted via owner-initiated invite.
   */
  requestedRole: GymRole;
}
