/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { ScheduledWorkoutStatus } from '../models/scheduled-workout-status';
import { WorkoutExerciseLogResponse } from '../models/workout-exercise-log-response';

/**
 * Returned from the coach-on-behalf write into a group-scheduled workout's per-athlete result
 * table (`POST /api/gyms/{gymId}/groups/{groupId}/scheduled-workouts/{scheduledWorkoutId}/results`).
 * Carries the persisted state of the target athlete's result row so the caller can update the
 * UI without a follow-up read. See `readmes/gym-ownership-requirements.md` §13.14.
 */
export interface GroupResultOnBehalfResponse {

  /**
   * The athlete this result belongs to (the target of the on-behalf write).
   */
  athleteId?: string | null;

  /**
   * Timestamp when the result transitioned to `Completed`. Null when status is not
   * Completed. Once stamped, preserved across subsequent edits while still Completed.
   */
  completedAt?: string | null;

  /**
   * When this result row was first inserted.
   */
  createdAt?: string;

  /**
   * Actual time the athlete spent on the workout. Null when no duration was recorded.
   */
  duration?: string | null;

  /**
   * Per-exercise log entries belonging to this result row.
   */
  exerciseLogs?: Array<WorkoutExerciseLogResponse> | null;

  /**
   * Unique identifier of the per-athlete result row.
   */
  id?: string | null;

  /**
   * Coach-entered notes on the athlete's behalf. Null when no note was set.
   */
  notes?: string | null;

  /**
   * The parent group-scheduled workout's id.
   */
  scheduledWorkoutId?: string | null;

  /**
   * Score string when present, format dictated by the parent workout's `ScoreType`.
   * Null when no score was recorded.
   */
  scoreResult?: string | null;

  /**
   * Result status (Pending / InProgress / Completed).
   */
  status?: ScheduledWorkoutStatus;

  /**
   * When this result row was last updated (server-stamped on every write).
   */
  updatedAt?: string;
}
