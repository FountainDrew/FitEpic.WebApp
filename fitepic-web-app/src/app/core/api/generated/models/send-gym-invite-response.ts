/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { GymRole } from '../models/gym-role';

/**
 * Response body for `POST /api/gyms/{gymId}/invites`. Deliberately derived only from
 * request inputs — never from server state — so the response is byte-identical regardless of
 * whether the supplied email matched a registered athlete. See requirements §6.2 for the
 * account-enumeration rationale. After a successful call the UI should re-fetch the gym's
 * invite list (`GET /api/gyms/{gymId}/invites`) to see the new pending row, since this
 * response does not echo back the server-assigned invite Id or any timestamps.
 */
export interface SendGymInviteResponse {

  /**
   * The email address that was submitted. Echoed so the UI can confirm what was sent.
   */
  email?: string | null;

  /**
   * The gym the invite was issued against. Echoes the request path parameter.
   */
  gymId?: string | null;

  /**
   * The role that was offered. Echoes the request body.
   */
  offeredRole?: GymRole;

  /**
   * Always the literal value `Accepted`. The endpoint deliberately does not distinguish
   * between "invite created" and "email did not match a registered athlete" — both outcomes
   * return the same value.
   */
  status?: string | null;
}
