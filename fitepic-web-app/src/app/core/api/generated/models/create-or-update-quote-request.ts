/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */


/**
 * Body for `POST .../quotes/mine` and `PUT .../quotes/mine/{id}` on both the
 * mobile and webapp surfaces. Validation is enforced server-side per the Quote of the Day
 * plan §5.4: `text` 1–1000 chars after trim; `author` 0–200 chars after trim
 * (empty string treated as null).
 */
export interface CreateOrUpdateQuoteRequest {

  /**
   * Optional author. Trimmed length must be 0–200 characters; empty / whitespace-only
   * is treated as null. Null when the athlete did not specify an author.
   */
  author?: string | null;

  /**
   * The quote body. Required. Trimmed length must be 1–1000 characters.
   */
  text: string;
}
