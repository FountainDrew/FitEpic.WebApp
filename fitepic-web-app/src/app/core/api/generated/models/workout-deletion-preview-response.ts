/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */


/**
 * Summary of everything a personal-workout delete would remove, for the web app's confirmation
 * dialog. Every count covers non-deleted rows only — rows already tombstoned are not re-reported.
 *             
 * Advisory: the subsequent `DELETE` recomputes the cascade from scratch, so these numbers can
 * be stale if the athlete's mobile client syncs in between. The delete response is authoritative.
 */
export interface WorkoutDeletionPreviewResponse {

  /**
   * Comments on the affected instances and group results.
   */
  commentCount?: number;

  /**
   * Completed scheduled instances. With `pendingInstanceCount` and
   * `dismissedInstanceCount` this partitions `scheduledInstanceCount`. Group-targeted
   * rows are classified by the caller's own per-athlete result when one exists, because the
   * parent row's status is only a template default.
   */
  completedInstanceCount?: number;

  /**
   * Dismissed scheduled instances.
   */
  dismissedInstanceCount?: number;

  /**
   * Earliest scheduled date among the affected instances. Null when there are none.
   */
  earliestScheduledDate?: string | null;

  /**
   * Logged exercise entries across all affected instances and group results.
   */
  exerciseLogCount?: number;

  /**
   * Per-athlete group result rows under the affected instances. Zero unless the workout was group-scheduled.
   */
  groupResultCount?: number;

  /**
   * Instances sitting on another athlete's calendar — rows scheduled for someone else, plus every
   * group-targeted row. Non-zero when the caller authored this workout and programmed it for a
   * client; deleting removes it from their calendar too. A subset of `scheduledInstanceCount`.
   */
  instancesOnOtherAthletesCalendars?: number;

  /**
   * Latest scheduled date among the affected instances. Null when there are none.
   */
  latestScheduledDate?: string | null;

  /**
   * Pending scheduled instances.
   */
  pendingInstanceCount?: number;

  /**
   * Reactions on the affected instances and group results.
   */
  reactionCount?: number;

  /**
   * Scheduled instances of this workout that would be soft-deleted.
   */
  scheduledInstanceCount?: number;

  /**
   * Exercises on the workout itself that would be soft-deleted.
   */
  workoutExerciseCount?: number;

  /**
   * The workout being previewed.
   */
  workoutId?: string | null;

  /**
   * The workout's display name. Null when the workout was saved without one.
   */
  workoutName?: string | null;
}
