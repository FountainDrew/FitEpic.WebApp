/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { ParsedWorkoutExerciseResponse } from '../models/parsed-workout-exercise-response';

/**
 * Structured representation of a workout parsed from raw text.
 */
export interface ParseWorkoutResponse {

  /**
   * Overall workout duration extracted from the workout text (time cap or total prescribed work,
   * e.g. "20 min AMRAP" → 00:20:00). Null when no overall workout duration was identified —
   * typical for StraightSets, open ForTime with no cap, or workouts whose duration depends solely
   * on athlete pace.
   */
  duration?: string | null;

  /**
   * Ordered list of exercises extracted from the workout, sorted by their position in the original text.
   */
  exercises?: Array<ParsedWorkoutExerciseResponse> | null;

  /**
   * Optional workout-level coaching notes or movement instructions extracted from the text. Null if none were found.
   */
  instructions?: string | null;

  /**
   * The workout name, if one could be identified (e.g., "Fran", "Cindy"). Null if the workout is unnamed.
   */
  name?: string | null;

  /**
   * The original workout text exactly as submitted by the caller.
   */
  originalText?: string | null;

  /**
   * The predicted score type for this workout. One of the `WorkoutScoreType` enum string values
   * (e.g., `TimeToComplete`, `RoundsAndReps`, `HeaviestLoad`). The mobile client parses
   * this directly via `Enum.Parse<WorkoutScoreType>()` — casing must match exactly.
   */
  predictedScoreType?: string | null;

  /**
   * Number of prescribed rounds extracted from the workout text. Null for all workout types except
   * `Intervals`, or when no explicit round count was found in the text.
   */
  roundCount?: number | null;

  /**
   * The structural type of the workout. Common values: `AMRAP`, `ForTime`, `EMOM`, `Strength`, `Chipper`.
   */
  workoutType?: string | null;
}
