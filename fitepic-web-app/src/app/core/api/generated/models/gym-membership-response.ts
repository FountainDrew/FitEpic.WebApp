/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { GymRole } from '../models/gym-role';

/**
 * Wire representation of an athlete's membership in a gym. The gym Owner does NOT have a
 * membership row — ownership is identified by `Gym.OwnerAthleteId`. Includes soft-deleted rows.
 */
export interface GymMembershipResponse {

  /**
   * Display name of the referenced athlete, projected from `AspNetUsers.Name` at query time.
   * Null when the athlete hasn't set a display name — clients should render a placeholder
   * (e.g., the email prefix or "Athlete") rather than the raw `AthleteId`.
   */
  athleteDisplayName?: string | null;

  /**
   * Email of the referenced athlete, projected from `AspNetUsers.Email` at query time.
   * Always present (every account has an email). Useful for staff to disambiguate members
   * with the same display name. Subject to the same mild staleness caveat as
   * FitEpic.Api.Models.Response.GymMembershipResponse.AthleteDisplayName.
   */
  athleteEmail?: string | null;

  /**
   * The member's athlete ID (canonical `AspNetUsers.Id`).
   */
  athleteId?: string | null;

  /**
   * The gym this membership belongs to.
   */
  gymId?: string | null;

  /**
   * Unique identifier (server-generated UUID).
   */
  id?: string | null;

  /**
   * Tombstone flag; `true` rows should be removed from local mobile state.
   */
  isDeleted?: boolean;

  /**
   * UTC timestamp when membership was first established.
   */
  joinedAt?: string;

  /**
   * Member's role: `Athlete`, `Coach`, or `Admin`.
   */
  role?: GymRole;

  /**
   * UTC timestamp of last modification. Drives delta-sync.
   */
  updatedAt?: string;
}
