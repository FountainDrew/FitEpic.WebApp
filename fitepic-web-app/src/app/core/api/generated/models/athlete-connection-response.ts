/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { AthleteConnectionStatus } from '../models/athlete-connection-status';
import { ConnectionPermissionLevel } from '../models/connection-permission-level';

/**
 * Represents an athlete-to-athlete connection record returned by the connections endpoints.
 * Includes soft-deleted records so mobile clients can reconcile their local state.
 */
export interface AthleteConnectionResponse {

  /**
   * The UTC timestamp when this connection was created.
   */
  createdAt?: string;

  /**
   * The ID of the athlete who grants visibility into their workouts.
   */
  grantingAthleteId?: string | null;

  /**
   * The unique identifier for this connection (server-generated UUID).
   */
  id?: string | null;

  /**
   * Whether this connection has been soft-deleted. A value of `true` means the connection
   * was removed and mobile clients should treat it as deleted in their local store.
   */
  isDeleted?: boolean;

  /**
   * The permission level granted by this connection. Effectively always `ViewWorkouts` in
   * the current model — the legacy `ProgramWorkouts` value is no longer issuable. Existing
   * rows with that value will be removed in the gym ownership data migration.
   */
  permissionLevel?: ConnectionPermissionLevel;

  /**
   * The ID of the athlete who requested the connection (the viewer in peer-share terms).
   */
  requestingAthleteId?: string | null;

  /**
   * The current status of the connection.
   */
  status?: AthleteConnectionStatus;

  /**
   * The UTC timestamp when this connection was last modified. Used for delta pull comparisons.
   */
  updatedAt?: string;
}
