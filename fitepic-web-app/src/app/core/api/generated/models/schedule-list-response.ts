/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { DashboardWorkoutCardResponse } from '../models/dashboard-workout-card-response';

/**
 * Response payload for `GET /api/webapp/schedule/list/v1`. Drives the web app's five
 * relative-date buckets (yesterday / today / tomorrow / future / past) with cursor-paged
 * past and future.
 */
export interface ScheduleListResponse {

  /**
   * Cards scheduled strictly after tomorrow, ascending by date then Pending before Completed
   * then by id. Bounded by the request's `futureLimit` (default 50, server-capped at 200).
   */
  future?: Array<DashboardWorkoutCardResponse> | null;

  /**
   * Opaque pagination cursor to fetch the next page of FitEpic.Api.Models.WebApp.Response.ScheduleListResponse.Future. Null when
   * exhausted. <strong>Clients must treat this string as a black box</strong> — never parse,
   * mutate, or compare it. Pass it back unchanged as the `futureCursor` query parameter
   * on the next request.
   */
  futureNextCursor?: string | null;

  /**
   * Cards scheduled strictly before yesterday, descending by date then Pending before
   * Completed then by id. Bounded by the request's `pastLimit` (default 50, server-capped
   * at 200).
   */
  past?: Array<DashboardWorkoutCardResponse> | null;

  /**
   * Opaque pagination cursor to fetch the next page of FitEpic.Api.Models.WebApp.Response.ScheduleListResponse.Past. Null when
   * exhausted. <strong>Clients must treat this string as a black box</strong> — never parse,
   * mutate, or compare it. Pass it back unchanged as the `pastCursor` query parameter
   * on the next request.
   */
  pastNextCursor?: string | null;

  /**
   * Cards scheduled for today. Same ordering rule as FitEpic.Api.Models.WebApp.Response.ScheduleListResponse.Yesterday.
   */
  today?: Array<DashboardWorkoutCardResponse> | null;

  /**
   * Today's calendar date in the athlete's profile timezone.
   */
  todayDate?: string;

  /**
   * Cards scheduled for tomorrow (FitEpic.Api.Models.WebApp.Response.ScheduleListResponse.TodayDate + 1). Same ordering rule as
   * FitEpic.Api.Models.WebApp.Response.ScheduleListResponse.Yesterday.
   */
  tomorrow?: Array<DashboardWorkoutCardResponse> | null;

  /**
   * Cards scheduled for yesterday (FitEpic.Api.Models.WebApp.Response.ScheduleListResponse.TodayDate − 1). Ordered Pending before
   * Completed; within each, stable by date then by id.
   */
  yesterday?: Array<DashboardWorkoutCardResponse> | null;
}
