/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */


/**
 * A single quote in the caller's visible pool (system seed quote OR a quote the caller
 * added themselves). Returned by the mobile and webapp Quote of the Day surfaces.
 */
export interface QuoteResponse {

  /**
   * The quote's author. Null when the athlete did not specify one.
   */
  author?: string | null;

  /**
   * The quote's unique identifier. For system quotes, derived deterministically from text + author.
   */
  id?: string | null;

  /**
   * True when the caller is the athlete who created this quote — controls edit/delete affordances on the client.
   */
  isOwnedByMe?: boolean;

  /**
   * True for seeded system quotes (read-only); false for user-added quotes.
   */
  isSystem?: boolean;

  /**
   * The quote body.
   */
  text?: string | null;

  /**
   * Last update timestamp on this quote row.
   */
  updatedAt?: string;
}
