/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GymJoinRequestResponse } from '../../models/gym-join-request-response';
import { GymJoinRequestStatus } from '../../models/gym-join-request-status';

export interface ApiGymsGymIdJoinRequestsGet$Params {

/**
 * Gym ID.
 */
  gymId: string;

/**
 * Optional status filter.
 */
  status?: GymJoinRequestStatus;

/**
 * Delta-sync watermark.
 */
  since?: string;
}

export function apiGymsGymIdJoinRequestsGet(http: HttpClient, rootUrl: string, params: ApiGymsGymIdJoinRequestsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<GymJoinRequestResponse>>> {
  const rb = new RequestBuilder(rootUrl, apiGymsGymIdJoinRequestsGet.PATH, 'get');
  if (params) {
    rb.path('gymId', params.gymId, {});
    rb.query('status', params.status, {});
    rb.query('since', params.since, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<GymJoinRequestResponse>>;
    })
  );
}

apiGymsGymIdJoinRequestsGet.PATH = '/api/gyms/{gymId}/join-requests';
