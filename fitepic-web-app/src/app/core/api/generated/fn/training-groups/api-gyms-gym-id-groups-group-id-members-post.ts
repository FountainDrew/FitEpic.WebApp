/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AssignTrainingGroupMemberRequest } from '../../models/assign-training-group-member-request';
import { TrainingGroupMembershipResponse } from '../../models/training-group-membership-response';

export interface ApiGymsGymIdGroupsGroupIdMembersPost$Params {

/**
 * Gym ID.
 */
  gymId: string;

/**
 * Group ID.
 */
  groupId: string;
  
    /**
     * Athlete to assign.
     */
    body?: AssignTrainingGroupMemberRequest
}

export function apiGymsGymIdGroupsGroupIdMembersPost(http: HttpClient, rootUrl: string, params: ApiGymsGymIdGroupsGroupIdMembersPost$Params, context?: HttpContext): Observable<StrictHttpResponse<TrainingGroupMembershipResponse>> {
  const rb = new RequestBuilder(rootUrl, apiGymsGymIdGroupsGroupIdMembersPost.PATH, 'post');
  if (params) {
    rb.path('gymId', params.gymId, {});
    rb.path('groupId', params.groupId, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TrainingGroupMembershipResponse>;
    })
  );
}

apiGymsGymIdGroupsGroupIdMembersPost.PATH = '/api/gyms/{gymId}/groups/{groupId}/members';
