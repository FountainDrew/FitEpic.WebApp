/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TrainingGroupResponse } from '../../models/training-group-response';

export interface ApiGymsGymIdGroupsGet$Params {

/**
 * Gym ID.
 */
  gymId: string;

/**
 * Delta-sync watermark.
 */
  since?: string;
}

export function apiGymsGymIdGroupsGet(http: HttpClient, rootUrl: string, params: ApiGymsGymIdGroupsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TrainingGroupResponse>>> {
  const rb = new RequestBuilder(rootUrl, apiGymsGymIdGroupsGet.PATH, 'get');
  if (params) {
    rb.path('gymId', params.gymId, {});
    rb.query('since', params.since, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TrainingGroupResponse>>;
    })
  );
}

apiGymsGymIdGroupsGet.PATH = '/api/gyms/{gymId}/groups';
