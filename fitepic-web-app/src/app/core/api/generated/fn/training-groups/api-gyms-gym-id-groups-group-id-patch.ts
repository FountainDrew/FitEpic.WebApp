/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TrainingGroupResponse } from '../../models/training-group-response';
import { UpdateTrainingGroupRequest } from '../../models/update-training-group-request';

export interface ApiGymsGymIdGroupsGroupIdPatch$Params {

/**
 * Gym ID (must match the group's gym).
 */
  gymId: string;

/**
 * Group ID.
 */
  groupId: string;
  
    /**
     * Updated name and description.
     */
    body?: UpdateTrainingGroupRequest
}

export function apiGymsGymIdGroupsGroupIdPatch(http: HttpClient, rootUrl: string, params: ApiGymsGymIdGroupsGroupIdPatch$Params, context?: HttpContext): Observable<StrictHttpResponse<TrainingGroupResponse>> {
  const rb = new RequestBuilder(rootUrl, apiGymsGymIdGroupsGroupIdPatch.PATH, 'patch');
  if (params) {
    rb.path('gymId', params.gymId, {});
    rb.path('groupId', params.groupId, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TrainingGroupResponse>;
    })
  );
}

apiGymsGymIdGroupsGroupIdPatch.PATH = '/api/gyms/{gymId}/groups/{groupId}';
