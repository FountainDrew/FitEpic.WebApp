/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateGymRequest } from '../../models/create-gym-request';
import { GymResponse } from '../../models/gym-response';

export interface ApiGymsPost$Params {
  
    /**
     * Gym name and optional description.
     */
    body?: CreateGymRequest
}

export function apiGymsPost(http: HttpClient, rootUrl: string, params?: ApiGymsPost$Params, context?: HttpContext): Observable<StrictHttpResponse<GymResponse>> {
  const rb = new RequestBuilder(rootUrl, apiGymsPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GymResponse>;
    })
  );
}

apiGymsPost.PATH = '/api/gyms';
