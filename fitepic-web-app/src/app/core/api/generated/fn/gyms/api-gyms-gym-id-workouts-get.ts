/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WorkoutResponse } from '../../models/workout-response';

export interface ApiGymsGymIdWorkoutsGet$Params {

/**
 * Gym ID.
 */
  gymId: string;

/**
 * Delta-sync watermark. Omit for a bootstrap pull.
 */
  since?: string;

/**
 * When `true`, includes rows with `IsArchived = true` in bootstrap pulls. Ignored on delta pulls. Defaults to `false`.
 */
  includeArchived?: boolean;

/**
 * When `true`, includes rows with `IsDeleted = true` in bootstrap pulls. Ignored on delta pulls (which always include tombstones). Defaults to `false`.
 */
  includeDeleted?: boolean;
}

export function apiGymsGymIdWorkoutsGet(http: HttpClient, rootUrl: string, params: ApiGymsGymIdWorkoutsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<WorkoutResponse>>> {
  const rb = new RequestBuilder(rootUrl, apiGymsGymIdWorkoutsGet.PATH, 'get');
  if (params) {
    rb.path('gymId', params.gymId, {});
    rb.query('since', params.since, {});
    rb.query('includeArchived', params.includeArchived, {});
    rb.query('includeDeleted', params.includeDeleted, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<WorkoutResponse>>;
    })
  );
}

apiGymsGymIdWorkoutsGet.PATH = '/api/gyms/{gymId}/workouts';
