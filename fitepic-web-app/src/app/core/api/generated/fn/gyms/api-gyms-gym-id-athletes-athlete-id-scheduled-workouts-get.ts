/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ScheduledWorkoutSyncResponse } from '../../models/scheduled-workout-sync-response';

export interface ApiGymsGymIdAthletesAthleteIdScheduledWorkoutsGet$Params {

/**
 * Gym ID scoping the read.
 */
  gymId: string;

/**
 * The athlete whose schedule is being viewed.
 */
  athleteId: string;

/**
 * Optional delta watermark.
 */
  since?: string;

/**
 * Optional inclusive `ScheduledDate` lower bound.
 */
  from?: string;

/**
 * Optional inclusive `ScheduledDate` upper bound.
 */
  to?: string;

/**
 * 1-based page number. Defaults to 1.
 */
  page?: number;

/**
 * Page size. Defaults to 100, capped at 500.
 */
  pageSize?: number;
}

export function apiGymsGymIdAthletesAthleteIdScheduledWorkoutsGet(http: HttpClient, rootUrl: string, params: ApiGymsGymIdAthletesAthleteIdScheduledWorkoutsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<ScheduledWorkoutSyncResponse>> {
  const rb = new RequestBuilder(rootUrl, apiGymsGymIdAthletesAthleteIdScheduledWorkoutsGet.PATH, 'get');
  if (params) {
    rb.path('gymId', params.gymId, {});
    rb.path('athleteId', params.athleteId, {});
    rb.query('since', params.since, {});
    rb.query('from', params.from, {});
    rb.query('to', params.to, {});
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ScheduledWorkoutSyncResponse>;
    })
  );
}

apiGymsGymIdAthletesAthleteIdScheduledWorkoutsGet.PATH = '/api/gyms/{gymId}/athletes/{athleteId}/scheduled-workouts';
