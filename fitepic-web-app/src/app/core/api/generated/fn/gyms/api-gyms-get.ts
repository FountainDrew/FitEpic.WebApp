/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GymResponse } from '../../models/gym-response';

export interface ApiGymsGet$Params {

/**
 * Filter mode: `owner` or `member`. Defaults to `member`.
 */
  role?: string;

/**
 * Return only rows updated after this UTC timestamp (delta sync). Omit for bootstrap.
 */
  since?: string;
}

export function apiGymsGet(http: HttpClient, rootUrl: string, params?: ApiGymsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<GymResponse>>> {
  const rb = new RequestBuilder(rootUrl, apiGymsGet.PATH, 'get');
  if (params) {
    rb.query('role', params.role, {});
    rb.query('since', params.since, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<GymResponse>>;
    })
  );
}

apiGymsGet.PATH = '/api/gyms';
