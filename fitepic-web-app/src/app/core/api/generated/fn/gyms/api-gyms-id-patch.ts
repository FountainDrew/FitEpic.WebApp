/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GymResponse } from '../../models/gym-response';
import { UpdateGymRequest } from '../../models/update-gym-request';

export interface ApiGymsIdPatch$Params {

/**
 * Gym ID.
 */
  id: string;
  
    /**
     * Updated name and description.
     */
    body?: UpdateGymRequest
}

export function apiGymsIdPatch(http: HttpClient, rootUrl: string, params: ApiGymsIdPatch$Params, context?: HttpContext): Observable<StrictHttpResponse<GymResponse>> {
  const rb = new RequestBuilder(rootUrl, apiGymsIdPatch.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GymResponse>;
    })
  );
}

apiGymsIdPatch.PATH = '/api/gyms/{id}';
