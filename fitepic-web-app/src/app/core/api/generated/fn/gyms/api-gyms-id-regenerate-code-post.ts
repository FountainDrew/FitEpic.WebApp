/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GymResponse } from '../../models/gym-response';

export interface ApiGymsIdRegenerateCodePost$Params {

/**
 * Gym ID.
 */
  id: string;
}

export function apiGymsIdRegenerateCodePost(http: HttpClient, rootUrl: string, params: ApiGymsIdRegenerateCodePost$Params, context?: HttpContext): Observable<StrictHttpResponse<GymResponse>> {
  const rb = new RequestBuilder(rootUrl, apiGymsIdRegenerateCodePost.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GymResponse>;
    })
  );
}

apiGymsIdRegenerateCodePost.PATH = '/api/gyms/{id}/regenerate-code';
