/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GymMembershipInviteResponse } from '../../models/gym-membership-invite-response';
import { GymMembershipInviteStatus } from '../../models/gym-membership-invite-status';

export interface ApiGymsGymIdInvitesGet$Params {

/**
 * Gym ID.
 */
  gymId: string;

/**
 * Optional status filter.
 */
  status?: GymMembershipInviteStatus;

/**
 * Delta-sync watermark.
 */
  since?: string;
}

export function apiGymsGymIdInvitesGet(http: HttpClient, rootUrl: string, params: ApiGymsGymIdInvitesGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<GymMembershipInviteResponse>>> {
  const rb = new RequestBuilder(rootUrl, apiGymsGymIdInvitesGet.PATH, 'get');
  if (params) {
    rb.path('gymId', params.gymId, {});
    rb.query('status', params.status, {});
    rb.query('since', params.since, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<GymMembershipInviteResponse>>;
    })
  );
}

apiGymsGymIdInvitesGet.PATH = '/api/gyms/{gymId}/invites';
