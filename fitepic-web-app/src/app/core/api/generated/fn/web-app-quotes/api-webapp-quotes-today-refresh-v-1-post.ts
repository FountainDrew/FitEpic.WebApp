/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { QuoteOfTheDayResponse } from '../../models/quote-of-the-day-response';

export interface ApiWebappQuotesTodayRefreshV1Post$Params {
}

export function apiWebappQuotesTodayRefreshV1Post(http: HttpClient, rootUrl: string, params?: ApiWebappQuotesTodayRefreshV1Post$Params, context?: HttpContext): Observable<StrictHttpResponse<QuoteOfTheDayResponse>> {
  const rb = new RequestBuilder(rootUrl, apiWebappQuotesTodayRefreshV1Post.PATH, 'post');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<QuoteOfTheDayResponse>;
    })
  );
}

apiWebappQuotesTodayRefreshV1Post.PATH = '/api/webapp/quotes/today/refresh/v1';
