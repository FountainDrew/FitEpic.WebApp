/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PagedQuotesResponse } from '../../models/paged-quotes-response';

export interface ApiWebappQuotesMineV1Get$Params {

/**
 * 1-based page number (default 1).
 */
  page?: number;

/**
 * Items per page (default 50, max 200).
 */
  pageSize?: number;
}

export function apiWebappQuotesMineV1Get(http: HttpClient, rootUrl: string, params?: ApiWebappQuotesMineV1Get$Params, context?: HttpContext): Observable<StrictHttpResponse<PagedQuotesResponse>> {
  const rb = new RequestBuilder(rootUrl, apiWebappQuotesMineV1Get.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PagedQuotesResponse>;
    })
  );
}

apiWebappQuotesMineV1Get.PATH = '/api/webapp/quotes/mine/v1';
