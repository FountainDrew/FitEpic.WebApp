/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateOrUpdateQuoteRequest } from '../../models/create-or-update-quote-request';
import { QuoteResponse } from '../../models/quote-response';

export interface ApiWebappQuotesMineIdV1Put$Params {

/**
 * The quote ID.
 */
  id: string;
  
    /**
     * The new text + author.
     */
    body?: CreateOrUpdateQuoteRequest
}

export function apiWebappQuotesMineIdV1Put(http: HttpClient, rootUrl: string, params: ApiWebappQuotesMineIdV1Put$Params, context?: HttpContext): Observable<StrictHttpResponse<QuoteResponse>> {
  const rb = new RequestBuilder(rootUrl, apiWebappQuotesMineIdV1Put.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<QuoteResponse>;
    })
  );
}

apiWebappQuotesMineIdV1Put.PATH = '/api/webapp/quotes/mine/{id}/v1';
