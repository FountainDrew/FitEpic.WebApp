/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { StandardExerciseResponsePagedResponse } from '../../models/standard-exercise-response-paged-response';

export interface StandardExercisesGet$Params {

/**
 * Optional UTC watermark. Returns only records where `UpdatedAt` is greater than this value.
 * Omit entirely to retrieve all records from the beginning of time (initial bootstrap pull).
 */
  since?: string;

/**
 * The 1-based page number to retrieve. Defaults to 1.
 */
  page?: number;

/**
 * The number of items per page. Defaults to 100.
 */
  pageSize?: number;
}

export function standardExercisesGet(http: HttpClient, rootUrl: string, params?: StandardExercisesGet$Params, context?: HttpContext): Observable<StrictHttpResponse<StandardExerciseResponsePagedResponse>> {
  const rb = new RequestBuilder(rootUrl, standardExercisesGet.PATH, 'get');
  if (params) {
    rb.query('since', params.since, {});
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StandardExerciseResponsePagedResponse>;
    })
  );
}

standardExercisesGet.PATH = '/StandardExercises';
