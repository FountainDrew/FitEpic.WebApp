/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ActivityFeedItemResponse } from '../../models/activity-feed-item-response';

export interface ApiMobileSocialFeedGet$Params {

/**
 * The page number (1-based). Defaults to 1.
 */
  page?: number;

/**
 * The number of items per page. Defaults to 20.
 */
  pageSize?: number;
}

export function apiMobileSocialFeedGet(http: HttpClient, rootUrl: string, params?: ApiMobileSocialFeedGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ActivityFeedItemResponse>>> {
  const rb = new RequestBuilder(rootUrl, apiMobileSocialFeedGet.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ActivityFeedItemResponse>>;
    })
  );
}

apiMobileSocialFeedGet.PATH = '/api/mobile/social/feed';
