/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReactionResponse } from '../../models/reaction-response';
import { ToggleReactionRequest } from '../../models/toggle-reaction-request';

export interface ApiSocialWorkoutsWorkoutLogIdReactionsPost$Params {

/**
 * The ID of the scheduled workout (completed log) to react to.
 */
  workoutLogId: string;
  
    /**
     * The reaction type to toggle.
     */
    body?: ToggleReactionRequest
}

export function apiSocialWorkoutsWorkoutLogIdReactionsPost(http: HttpClient, rootUrl: string, params: ApiSocialWorkoutsWorkoutLogIdReactionsPost$Params, context?: HttpContext): Observable<StrictHttpResponse<ReactionResponse>> {
  const rb = new RequestBuilder(rootUrl, apiSocialWorkoutsWorkoutLogIdReactionsPost.PATH, 'post');
  if (params) {
    rb.path('workoutLogId', params.workoutLogId, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReactionResponse>;
    })
  );
}

apiSocialWorkoutsWorkoutLogIdReactionsPost.PATH = '/api/social/workouts/{workoutLogId}/reactions';
