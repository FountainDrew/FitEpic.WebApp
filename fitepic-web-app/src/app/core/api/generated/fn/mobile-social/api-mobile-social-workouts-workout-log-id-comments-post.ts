/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AddCommentRequest } from '../../models/add-comment-request';
import { WorkoutCommentResponse } from '../../models/workout-comment-response';

export interface ApiMobileSocialWorkoutsWorkoutLogIdCommentsPost$Params {

/**
 * The ID of the scheduled workout (completed log) to comment on.
 */
  workoutLogId: string;
  
    /**
     * The comment text.
     */
    body?: AddCommentRequest
}

export function apiMobileSocialWorkoutsWorkoutLogIdCommentsPost(http: HttpClient, rootUrl: string, params: ApiMobileSocialWorkoutsWorkoutLogIdCommentsPost$Params, context?: HttpContext): Observable<StrictHttpResponse<WorkoutCommentResponse>> {
  const rb = new RequestBuilder(rootUrl, apiMobileSocialWorkoutsWorkoutLogIdCommentsPost.PATH, 'post');
  if (params) {
    rb.path('workoutLogId', params.workoutLogId, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WorkoutCommentResponse>;
    })
  );
}

apiMobileSocialWorkoutsWorkoutLogIdCommentsPost.PATH = '/api/mobile/social/workouts/{workoutLogId}/comments';
