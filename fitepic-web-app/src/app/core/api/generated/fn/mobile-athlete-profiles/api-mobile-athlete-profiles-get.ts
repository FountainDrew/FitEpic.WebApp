/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AthleteProfileResponse } from '../../models/athlete-profile-response';

export interface ApiMobileAthleteProfilesGet$Params {

/**
 * Return only profiles for connections updated after this ISO 8601 timestamp. Omit for a full pull.
 */
  since?: string;
}

export function apiMobileAthleteProfilesGet(http: HttpClient, rootUrl: string, params?: ApiMobileAthleteProfilesGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<AthleteProfileResponse>>> {
  const rb = new RequestBuilder(rootUrl, apiMobileAthleteProfilesGet.PATH, 'get');
  if (params) {
    rb.query('since', params.since, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<AthleteProfileResponse>>;
    })
  );
}

apiMobileAthleteProfilesGet.PATH = '/api/mobile/athlete-profiles';
