/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WorkoutDeletionResultResponse } from '../../models/workout-deletion-result-response';

export interface ApiWebappWorkoutsWorkoutIdV1Delete$Params {

/**
 * The personal workout to delete.
 */
  workoutId: string;
}

export function apiWebappWorkoutsWorkoutIdV1Delete(http: HttpClient, rootUrl: string, params: ApiWebappWorkoutsWorkoutIdV1Delete$Params, context?: HttpContext): Observable<StrictHttpResponse<WorkoutDeletionResultResponse>> {
  const rb = new RequestBuilder(rootUrl, apiWebappWorkoutsWorkoutIdV1Delete.PATH, 'delete');
  if (params) {
    rb.path('workoutId', params.workoutId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WorkoutDeletionResultResponse>;
    })
  );
}

apiWebappWorkoutsWorkoutIdV1Delete.PATH = '/api/webapp/workouts/{workoutId}/v1';
