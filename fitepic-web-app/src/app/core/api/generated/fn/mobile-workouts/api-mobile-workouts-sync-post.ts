/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SyncBatchResponse } from '../../models/sync-batch-response';
import { WorkoutRequest } from '../../models/workout-request';

export interface ApiMobileWorkoutsSyncPost$Params {
  
    /**
     * The flat list of workout payloads to sync. Set `isDeleted: true` to soft-delete.
     */
    body?: Array<WorkoutRequest>
}

export function apiMobileWorkoutsSyncPost(http: HttpClient, rootUrl: string, params?: ApiMobileWorkoutsSyncPost$Params, context?: HttpContext): Observable<StrictHttpResponse<SyncBatchResponse>> {
  const rb = new RequestBuilder(rootUrl, apiMobileWorkoutsSyncPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SyncBatchResponse>;
    })
  );
}

apiMobileWorkoutsSyncPost.PATH = '/api/mobile/workouts/sync';
