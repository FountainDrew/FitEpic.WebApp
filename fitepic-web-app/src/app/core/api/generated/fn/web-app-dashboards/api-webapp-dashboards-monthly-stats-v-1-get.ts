/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MonthlyStatsResponse } from '../../models/monthly-stats-response';

export interface ApiWebappDashboardsMonthlyStatsV1Get$Params {

/**
 * Calendar year (athlete profile timezone). Omit to default to the current year.
 */
  year?: number;

/**
 * Calendar month, 1..12 (athlete profile timezone). Omit to default to the current month.
 */
  month?: number;
}

export function apiWebappDashboardsMonthlyStatsV1Get(http: HttpClient, rootUrl: string, params?: ApiWebappDashboardsMonthlyStatsV1Get$Params, context?: HttpContext): Observable<StrictHttpResponse<MonthlyStatsResponse>> {
  const rb = new RequestBuilder(rootUrl, apiWebappDashboardsMonthlyStatsV1Get.PATH, 'get');
  if (params) {
    rb.query('year', params.year, {});
    rb.query('month', params.month, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MonthlyStatsResponse>;
    })
  );
}

apiWebappDashboardsMonthlyStatsV1Get.PATH = '/api/webapp/dashboards/monthly-stats/v1';
