/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ScheduledWorkoutResponsePagedResponse } from '../../models/scheduled-workout-response-paged-response';

export interface ApiMobileAthletesIdScheduledworkoutsGet$Params {

/**
 * The ID of the target athlete whose programmed workouts to retrieve.
 */
  id: string;

/**
 * 1-based page number. Defaults to 1.
 */
  page?: number;

/**
 * Number of records per page. Defaults to 100, capped at 500.
 */
  pageSize?: number;
}

export function apiMobileAthletesIdScheduledworkoutsGet(http: HttpClient, rootUrl: string, params: ApiMobileAthletesIdScheduledworkoutsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<ScheduledWorkoutResponsePagedResponse>> {
  const rb = new RequestBuilder(rootUrl, apiMobileAthletesIdScheduledworkoutsGet.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ScheduledWorkoutResponsePagedResponse>;
    })
  );
}

apiMobileAthletesIdScheduledworkoutsGet.PATH = '/api/mobile/athletes/{id}/scheduledworkouts';
