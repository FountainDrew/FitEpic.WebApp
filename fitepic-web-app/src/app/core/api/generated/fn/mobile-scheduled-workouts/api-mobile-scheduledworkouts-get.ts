/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ScheduledWorkoutResponse } from '../../models/scheduled-workout-response';

export interface ApiMobileScheduledworkoutsGet$Params {

/**
 * Optional single date in `YYYY-MM-DD` format. Ignored if `from`/`to` are both present.
 */
  date?: string;

/**
 * Optional inclusive lower bound for `ScheduledDate`. Requires `to`.
 */
  from?: string;

/**
 * Optional inclusive upper bound for `ScheduledDate`. Requires `from`.
 */
  to?: string;
}

export function apiMobileScheduledworkoutsGet(http: HttpClient, rootUrl: string, params?: ApiMobileScheduledworkoutsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ScheduledWorkoutResponse>>> {
  const rb = new RequestBuilder(rootUrl, apiMobileScheduledworkoutsGet.PATH, 'get');
  if (params) {
    rb.query('date', params.date, {});
    rb.query('from', params.from, {});
    rb.query('to', params.to, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ScheduledWorkoutResponse>>;
    })
  );
}

apiMobileScheduledworkoutsGet.PATH = '/api/mobile/scheduledworkouts';
