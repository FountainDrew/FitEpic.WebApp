/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ScheduledWorkoutRequest } from '../../models/scheduled-workout-request';
import { SyncBatchResponse } from '../../models/sync-batch-response';

export interface ApiMobileScheduledworkoutsSyncPost$Params {
  
    /**
     * A plain list of scheduled workout payloads. Set `isDeleted: true` on any item to soft-delete it.
     */
    body?: Array<ScheduledWorkoutRequest>
}

export function apiMobileScheduledworkoutsSyncPost(http: HttpClient, rootUrl: string, params?: ApiMobileScheduledworkoutsSyncPost$Params, context?: HttpContext): Observable<StrictHttpResponse<SyncBatchResponse>> {
  const rb = new RequestBuilder(rootUrl, apiMobileScheduledworkoutsSyncPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SyncBatchResponse>;
    })
  );
}

apiMobileScheduledworkoutsSyncPost.PATH = '/api/mobile/scheduledworkouts/sync';
