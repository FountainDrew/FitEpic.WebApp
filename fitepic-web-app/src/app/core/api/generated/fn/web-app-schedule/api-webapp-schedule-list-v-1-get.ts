/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ScheduleListResponse } from '../../models/schedule-list-response';

export interface ApiWebappScheduleListV1Get$Params {

/**
 * Maximum rows in the past bucket. Defaults to 50, server-clamped to a maximum of 200.
 */
  pastLimit?: number;

/**
 * Maximum rows in the future bucket. Defaults to 50, server-clamped to a maximum of 200.
 */
  futureLimit?: number;

/**
 * Opaque cursor returned by a prior response's `pastNextCursor`. Omit to fetch from the start of the past bucket.
 */
  pastCursor?: string;

/**
 * Opaque cursor returned by a prior response's `futureNextCursor`. Omit to fetch from the start of the future bucket.
 */
  futureCursor?: string;
}

export function apiWebappScheduleListV1Get(http: HttpClient, rootUrl: string, params?: ApiWebappScheduleListV1Get$Params, context?: HttpContext): Observable<StrictHttpResponse<ScheduleListResponse>> {
  const rb = new RequestBuilder(rootUrl, apiWebappScheduleListV1Get.PATH, 'get');
  if (params) {
    rb.query('pastLimit', params.pastLimit, {});
    rb.query('futureLimit', params.futureLimit, {});
    rb.query('pastCursor', params.pastCursor, {});
    rb.query('futureCursor', params.futureCursor, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ScheduleListResponse>;
    })
  );
}

apiWebappScheduleListV1Get.PATH = '/api/webapp/schedule/list/v1';
