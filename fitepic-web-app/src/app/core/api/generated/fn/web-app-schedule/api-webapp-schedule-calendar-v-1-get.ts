/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CalendarScheduleResponse } from '../../models/calendar-schedule-response';

export interface ApiWebappScheduleCalendarV1Get$Params {

/**
 * Calendar year in the athlete's profile timezone (≥ 1900).
 */
  year?: number;

/**
 * Calendar month, 1..12, in the athlete's profile timezone.
 */
  month?: number;
}

export function apiWebappScheduleCalendarV1Get(http: HttpClient, rootUrl: string, params?: ApiWebappScheduleCalendarV1Get$Params, context?: HttpContext): Observable<StrictHttpResponse<CalendarScheduleResponse>> {
  const rb = new RequestBuilder(rootUrl, apiWebappScheduleCalendarV1Get.PATH, 'get');
  if (params) {
    rb.query('year', params.year, {});
    rb.query('month', params.month, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CalendarScheduleResponse>;
    })
  );
}

apiWebappScheduleCalendarV1Get.PATH = '/api/webapp/schedule/calendar/v1';
