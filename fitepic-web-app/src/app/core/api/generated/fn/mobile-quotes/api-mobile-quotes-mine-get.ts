/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PagedQuotesResponse } from '../../models/paged-quotes-response';

export interface ApiMobileQuotesMineGet$Params {

/**
 * 1-based page number (default 1).
 */
  page?: number;

/**
 * Items per page (default 50, max 200).
 */
  pageSize?: number;
}

export function apiMobileQuotesMineGet(http: HttpClient, rootUrl: string, params?: ApiMobileQuotesMineGet$Params, context?: HttpContext): Observable<StrictHttpResponse<PagedQuotesResponse>> {
  const rb = new RequestBuilder(rootUrl, apiMobileQuotesMineGet.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PagedQuotesResponse>;
    })
  );
}

apiMobileQuotesMineGet.PATH = '/api/mobile/quotes/mine';
