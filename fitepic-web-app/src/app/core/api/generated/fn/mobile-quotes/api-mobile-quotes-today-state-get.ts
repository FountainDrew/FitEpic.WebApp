/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { QuoteOfTheDayResponse } from '../../models/quote-of-the-day-response';

export interface ApiMobileQuotesTodayStateGet$Params {

/**
 * Last `UpdatedAt` the client saw. Omit on first launch to receive the current state unconditionally.
 */
  since?: string;
}

export function apiMobileQuotesTodayStateGet(http: HttpClient, rootUrl: string, params?: ApiMobileQuotesTodayStateGet$Params, context?: HttpContext): Observable<StrictHttpResponse<QuoteOfTheDayResponse>> {
  const rb = new RequestBuilder(rootUrl, apiMobileQuotesTodayStateGet.PATH, 'get');
  if (params) {
    rb.query('since', params.since, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<QuoteOfTheDayResponse>;
    })
  );
}

apiMobileQuotesTodayStateGet.PATH = '/api/mobile/quotes/today/state';
