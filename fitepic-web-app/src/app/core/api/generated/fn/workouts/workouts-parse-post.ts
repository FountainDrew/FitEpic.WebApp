/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ParseWorkoutRequest } from '../../models/parse-workout-request';
import { ParseWorkoutResponse } from '../../models/parse-workout-response';

export interface WorkoutsParsePost$Params {
  
    /**
     * The raw workout text to parse.
     */
    body?: ParseWorkoutRequest
}

export function workoutsParsePost(http: HttpClient, rootUrl: string, params?: WorkoutsParsePost$Params, context?: HttpContext): Observable<StrictHttpResponse<ParseWorkoutResponse>> {
  const rb = new RequestBuilder(rootUrl, workoutsParsePost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ParseWorkoutResponse>;
    })
  );
}

workoutsParsePost.PATH = '/workouts/parse';
