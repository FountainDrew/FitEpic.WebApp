/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WorkoutResponsePagedResponse } from '../../models/workout-response-paged-response';

export interface WorkoutsGet$Params {

/**
 * Return only records updated after this ISO 8601 timestamp. Omit for a full pull.
 */
  since?: string;

/**
 * 1-based page number. Defaults to 1.
 */
  page?: number;

/**
 * Number of records per page. Defaults to 100, capped at 500.
 */
  pageSize?: number;
}

export function workoutsGet(http: HttpClient, rootUrl: string, params?: WorkoutsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<WorkoutResponsePagedResponse>> {
  const rb = new RequestBuilder(rootUrl, workoutsGet.PATH, 'get');
  if (params) {
    rb.query('since', params.since, {});
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WorkoutResponsePagedResponse>;
    })
  );
}

workoutsGet.PATH = '/workouts';
