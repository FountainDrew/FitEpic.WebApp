/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GymMembershipResponse } from '../../models/gym-membership-response';

export interface ApiGymsGymIdMembersGet$Params {

/**
 * Gym ID.
 */
  gymId: string;

/**
 * Delta-sync watermark.
 */
  since?: string;
}

export function apiGymsGymIdMembersGet(http: HttpClient, rootUrl: string, params: ApiGymsGymIdMembersGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<GymMembershipResponse>>> {
  const rb = new RequestBuilder(rootUrl, apiGymsGymIdMembersGet.PATH, 'get');
  if (params) {
    rb.path('gymId', params.gymId, {});
    rb.query('since', params.since, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<GymMembershipResponse>>;
    })
  );
}

apiGymsGymIdMembersGet.PATH = '/api/gyms/{gymId}/members';
